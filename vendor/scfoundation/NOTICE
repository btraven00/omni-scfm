Vendored from biomap-research/scFoundation @ 397631c495eddf9ad6644fc00c6ea8139e651245
(Apache License 2.0 — see ./LICENSE; model weights under a separate MODEL_LICENSE).

  scfoundation_gears/  = scFoundation's forked GEARS (gears.version == 0.0.2), required
                         by run_scfoundation.py (asserts that version). data/ blobs
                         (demo.h5ad, gene2go.pkl) excluded — gene2go is side-loaded.
  model/               = scFoundation encoder code (maeautobin) loaded as the GEARS
                         backbone via load_path=<models.ckpt> (checkpoint side-loaded).

Modifications (Apache-2.0 sec.4 — stating changes):
  scfoundation_gears/gears/model.py — one memory-only patch (commented in place): when
    the encoder is frozen (finetune_method='frozen'), its forward is wrapped in
    torch.no_grad() to drop a ~15GB activation graph that is retained but never used
    (mae_autobin.py:126 forces requires_grad for an attn-map feature unused in training).
    Numerically identical (see the in-code proof) — it just lets scFoundation train at
    the paper's batch_size=6 on a 24GB GPU.
